# Slice 002 — Resistance request preparation

This additive pilot consumes accepted Slice 001 commit
`97950ef55a6a2cc5a54752d1ec54243de327a646` without changing its files.
Trader source revision is `af9f8a392f95363e86c5636aefa5c55494715f6c`;
evaluated baseline remains `46828c1658b5328f65fd23ad2e350a19e54039ae`.
The operative authority is `evidence/slice002/po-authorization.txt`.

The observed run stopped correctly at native preflight: 5604 prompt estimate +
1600 requested output + 800 safety = 8004, which fails the strict <8000 gate.
No request shrinking, automatic retry, Groq call or audit execution followed.
This is PARTIAL, not completion of the requested end-to-end segment.

## Execution and authority

The real run used isolated Codex Advisor sessions, one isolated Codex candidate
computation, and a deterministic CODE_EXECUTOR backend for published local
validation/preflight. Model tools were disabled. Control Plane independently
compared the complete generated candidate with the exact authorized transformation
before writing only under `candidates/slice002/`. Supporting files are unchanged
copies, including the A5 schema: copying that reference grants no A5 authority.

Advisor proposes destinations; `applicable` and `route` check current state.
The observed route was Advisor → CodeExecutor → Advisor → CodeExecutor → Advisor
→ PO_REQUIRED. Development is applicable to a material specification gap, but
was neither necessary nor exercised. There is no role-coverage dispatch.

All code, roles and evidence in this pilot are proposals/observations, not a
Trader authority source. The final Advisor explicitly distinguished a successful
diagnostic operation from a failed readiness gate. Its full findings, including
the limits of the final projected context, remain in the execution evidence.

## Protection and persistence

`trader-reference` is a separate no-checkout read-only reference clone.
`.runtime/slice002/trader-snapshot` is a Control Plane-owned byte-identical export
of the 238 pinned tracked files. Native validator imports use that export, with
bytecode writes disabled and Python socket operations denied. Only explicitly
allowlisted local validation functions run; provider/audit entrypoints are mocked
to fail if called. Baseline evidence is read through `git show` and verified by
the native manifest reader. No historical A4 raw output is submitted to a resolver.

The durable SQLite ledger is `.runtime/slice002/ledger.sqlite`; each validated
context/result also has a durable spool under `evidence/slice002/executions`.
The same task/execution identities are reused on restart. Recover before dispatch;
uncertain/isolated executions are not automatically regenerated by this pilot.
The immutable candidate path rejects different replacement bytes. Material source
changes fail closed. A retry would not solve the preflight blocker and is forbidden.

Keep the ledger and spool together. `ledger-export.json` is a reviewable evidence
export, not an automatic authority-restoration mechanism. No rollback of Trader is
needed: candidate activation and all Trader write mechanisms are absent. Recovery
retains the inactive candidate and terminal STOP for explicit later disposition.

## Verification and reproduction

`python -m unittest discover -s tests -q` runs 51 portable offline tests.
The real publication validation additionally ran 135 published tooling tests,
including 35 resolver tests, using free pytest installed only in the ignored
runtime directory. Its initial temporary-directory permission error is preserved
as environment evidence; the corrected CP-owned temporary directory run passed.

Live commands require the already authenticated desktop Codex CLI, a verified
fresh included-capacity snapshot, the source export matching `source-manifest.json`,
and the local source clone for pinned baseline reads. No paid fallback is supported.
The cost snapshot is operator-supplied evidence, not a billing API or unlimited quota
guarantee. Do not fabricate it or reuse it after expiry.

The run entrypoint is `tdt_control_plane.resistance_pilot` with `--root`, `--codex`
and `--cost`. **Do not start a new logical run to bypass this recorded STOP.**
`tdt_control_plane.resistance_recovery --root ROOT` replays the existing persisted
run with fresh execution/subprocess APIs disabled and proves duplicate/no-op;
it does not repeat validation, Codex, Groq or historical audit work.

`evidence/slice002/FINAL-REPORT.md` and `implementation-report.json` contain the
consolidated STOP gate, paths, identities, digests, results and limitations.
Source and candidate hashes preserve exact bytes across clean clones through
directory-local Git attributes. Slice 001 remains a consumed bounded dependency;
this branch does not merge it, reopen it, or increase its operational claims.
